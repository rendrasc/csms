# Cosmos Explorer 3D

## 🚀 Quick Start
1. Install dependencies:
   ```bash
   npm install
   ```
2. Run locally:
   ```bash
   npm run dev
   ```

## ☁️ Deployment to Vercel

### Option 1: Via Command Line (Fastest)
This project includes a pre-configured `vercel.json`.
Simply run:
```bash
npx vercel
```
Follow the prompts (keep default settings) to deploy.

### Option 2: Via GitHub (Recommended for updates)
1. Push this folder to a GitHub repository.
2. Log in to [Vercel.com](https://vercel.com).
3. Click "Add New Project" and select your repository.
4. Vercel will automatically detect the Vite framework and deploy it.
