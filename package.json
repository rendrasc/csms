{
  "name": "cosmos-explorer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "homepage": "https://rendrasc.github.io/csms",  // <--- ADD THIS
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "predeploy": "npm run build",      // <--- ADD THIS (Builds before deploying)
    "deploy": "gh-pages -d dist"       // <--- ADD THIS (Pushes 'dist' folder to GitHub)
  },
  "dependencies": {
    // ... (keep your existing dependencies)
  },
  "devDependencies": {
    // ... (keep your existing devDependencies including gh-pages)
  }
}